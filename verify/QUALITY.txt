# QUALITY.txt — rolling summary (LANE=VERIFY) — regenerated each pass
# Last pass: 2026-08-23 catch-up (Phases 8+10 committed; ABI v1.2/v1.3 ratified; SVC display landed)

| repo | head reviewed | mechanical | open BLOCKERs | open MAJORs | trend |
|---|---|---|---|---|---|
| MAIN  | 4e5b4fd (Phase 8 timer/PIC + virtio-blk backend; Phase 10 tools/img + p8b persistence + README; ABIs v1.2/v1.3 ratified; all-9-gates-green claimed; Makefile cross-gate deps fix) | per-file C++ syntax sweep: ZERO failures across core/ incl. virtio_blk.cc/input.cc/preempt.cc | 6: F12 devblk wraparound OOB · F13 LOGIN owner/binder escalation · F16 routed_rw reply clamp · F18 §7 silent default · F31 kern_blk_* ungated (now hits PERSISTENT disk) · F32 uid stamping absent | 2: F42-class none here… actually MAIN MAJORs: F4 hosteng stale · F37 ppa over-broad caps · NEW F45 virtio-blk completion path broken (stale poll / ignored status byte / missing NEXT flag) | Phases 0–5,7,8,10 gates GREEN; six security blockers persist in committed code |
| SVC   | 00f3bc6 (net E2E + race-hardening + multiuser rooting hardening + NEW display service on §9.FB; fs/display split into own modules) | go vet clean; FULL suite -race -count=1 GREEN (console/init/login/net/shell in parent module; fs + display/terminal as sub-modules) | 0 | 0 (F35 races closed @dadeaf4; F26 rooting CLOSED @a32873b; F17 kernel-side resolved in MAIN tree) | healthiest lane; Phase-9 net + §9.FB display both green |
| TLS   | b75d0f5 (+uncommitted MEMORY.md broadcast) | vet/test green incl. hvtest + golden files | 0 | 0 | healthy |

Closed ledger: F1 F5 F11 F15 F20 F22 F26 F33 F3(all sub-items).
Open BLOCKERs (6, all MAIN): F12 F13 F16 F18 F31 F32 — the multiuser-defeating
trio (F31/F32/F13) is the priority order.
Open MAJORs: F4 · F37 · F45 (NEW: virtio-blk shim completion path — stale
poll skip, device status byte never checked, missing NEXT flag on desc 1;
green p8b gate masked by QEMU fast sync completions) · F42 (SVC fs build
break at dadeaf4 — recheck against a32873b working tree which builds green,
so possibly resolved-in-tree pending verification from clean clone).
Watchlist: F6 F7 F8 F9 F14 F19 F21 F24 F25 F34 F36 F38 F43 F44.

Process notes:
- fs + display are now separate Go modules under services/ — parent ./...
  skips them; mechanical runs must target each module root.
- Fleet broadcasts continue to sync into all worktrees' MEMORY.md
  (verify worktree's copy left uncommitted by design).
