# QUALITY.txt — rolling summary (LANE=VERIFY) — regenerated each pass
# Last pass: 2026-08-28 08:50 UTC — MAIN@f6632b5 (VFIO DELETED at committed HEAD) · SVC@d789b35+dirty (F223 fixed)

| repo | head reviewed | mechanical | open BLOCKERs | open MAJORs | trend |
|---|---|---|---|---|---|
| MAIN  | f6632b5 (committed, clean working tree) | committed HEAD builds clean (kernel.so 1.2MB); `make` PASS; test-g1 PASS; test-p4 PASS | **1** (F231 — VFIO subsystem deleted at committed HEAD; all Phase 11 code gone) | 0 | **REGRESSION**: f6632b5 deleted core/vfio.cc, core/pci.cc, services/graphics/, guests/p11/, fuzz targets |
| SVC   | d789b35 (committed) + dirty 4 files (netclient.go Yield fix + netconn_test.go + fs/server.go + tcp_test.go) | go vet clean; full suite -race green: fs 5.0s, net 3.8s, guests/lib 1.2s, console/init/login/shell green | 0 | 0 | healthy; F223 CLOSED |
| TLS   | 1ccd2bf (no delta) | go vet clean; img + hvtest test PASS | 0 | 0 | healthy; stable |

## Phase 11 gate — DELETED (not applicable)
test-p11 target removed from Makefile; core/vfio.cc, core/pci.cc, services/graphics/,
guests/p11/ all deleted in commit f6632b5. Phase 11 must be re-landed from bca4a40.

## Test matrix — PARTIAL (g1/p4 green; p10/p11 not run this pass)
- Committed HEAD f6632b5: test-g1 PASS, test-p4 PASS (TCG).
- test-p10: not run this pass (requires disk build + QEMU).
- test-p11: target deleted.

## Open BLOCKERs
### Committed HEAD:
- F231 BLOCKER: VFIO subsystem deleted at committed HEAD f6632b5. core/vfio.cc (-580 lines), core/pci.cc (-251 lines), core/pci.h, core/vfio.h, services/graphics/, guests/p11/, guests/pci_dump/, guests/lib/vfio_fuzz_test.go, guests/lib/chaos_gate_test.go all removed. test-p11 target removed from Makefile. Phase 11 absent from tree.

## Fuzz target results (this pass) — NO TARGETS
All fuzz targets (FuzzVfioBarMapping, FuzzVfioDoorbellState, FuzzPciConfigOffset,
FuzzChaosGate) were deleted with guests/lib/vfio_fuzz_test.go in f6632b5.
FuzzKFSReplay (SVC) and FuzzParseCanonicalHeader (SVC) still exist but not run this pass.

## Deltas reviewed this poll
- MAIN: committed HEAD f6632b5 vs bca4a40 — full VFIO deletion confirmed via `git diff bca4a40 f6632b5 --stat` (core/vfio.cc -580, core/pci.cc -251, services/graphics/ gone, guests/p11/ gone, fuzz targets gone). Working tree clean (matches HEAD).
- SVC: F223 fix confirmed (NetClient.Yield delegates to Client.Bus.Yield); new netconn_test.go (mockNetServer); fs/server.go home redirect; tcp_test.go TestTCPSimultaneousClose. Full suite green.

## Key observations
- v2 ABI transition latent: all services abi_ver=1, kernel abi_ver=2, no enforcement yet.
- F231 is a committed-HEAD regression, not just working-tree. Requires revert or re-land of bca4a40.
- SVC lane healthy after F223 fix.

## Process notes
- Never emitting ALL PHASES COMPLETE (coordinator decides).
- Branch heads: MAIN@f6632b5 (clean), SVC@d789b35 (dirty 4 files), TLS@1ccd2bf.
- F187-F195 series now moot (code deleted); F231 supersedes them.
