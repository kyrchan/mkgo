# QUALITY.txt — rolling summary (LANE=VERIFY) — regenerated each pass
# Last pass: 2026-08-28 23:10 UTC+8 — re-poll13 · VFIO re-review complete; fuzz 5/5 PASS; ledger updated

| repo | head reviewed | mechanical | open BLOCKERs | open MAJORs | trend |
|---|---|---|---|---|---|
| MAIN  | f6632b5 (committed, clean working tree) | `make clean && make` PASS; `test-p11` PASS (TCG); fuzz 5/5 PASS | 1 new (F241) · 9 committed (F187-F195) | 2 new (F242-F243) | new VFIO findings filed; ledger updated |
| SVC   | fb0a314 (committed) | per-module `go test -count=1` PASS; 7 net fuzz PASS; go vet clean | 0 | 0 | healthy; stable |
| TLS   | 1ccd2bf (committed, no delta) | `go test -count=1 ./tools/img` PASS 1.6s; go vet clean | 0 | 0 | stable |

## Phase 11 gate — GREEN (committed HEAD f6632b5)
test-p11: PASS ("TEST PASS (p11 VFIO smoke)") — verified this poll. BDF 0:1:0 NOT enumerated (no VVGA dirty delta at committed HEAD); cap-denied path exercised (no CAP_PCI/CAP_FB → all ops denied as expected).
VFIO foundation at committed HEAD: BAR mapping (8 slots), doorbells (4), IOMMU domain tracking (256 pages/domain), MSI-X programming scaffolding, framebuffer control, devman enumeration, session cleanup.

## Test matrix — GREEN @f6632b5 (committed, clean working tree)
- Committed HEAD f6632b5: test-p11 PASS — TCG (KVM env blocked by stale locks).
- Working tree = committed HEAD; no separate dirty-tree verification needed.
- p4/p10: trusted from prior clean-room verification (F198/F201).

## Open BLOCKERs
- **F241 (NEW)**: vfio_map_bar/vfio_bind_irq/kern_pci_enable_busmaster missing device-assignment check — any CAP_PCI session can access any device regardless of registry assignment (ABI §12 deviation + capability-check gap)
- F187-F195: committed-HEAD VFIO foundation limits (previously filed)

## Open MAJORs
- **F242 (NEW)**: vfio_map_bar BAR window is a shadow buffer, not real MMIO passthrough — guest writes don't reach physical device (ABI §12 "Guest accesses BAR MMIO through this window" currently false for non-FB devices)
- **F243 (NEW)**: vfio_iommu_permits defined but never called — IOMMU enforcement is dead code

## Open MINORs
- F244: vfio.h duplicate declarations (vfio_iommu_permits, vfio_recover_after_flr, vfio_session_cleanup)
- F245: alloc_window bump allocator never reclaims freed windows
- F246: next_handle uint32_t wrap after 2^32 binds
- F247: fb_has_display set but never read (dead state)
- F248: pci_flr/pci_msix_find capability chain walk — no table-size validation

## Fuzz target results (re-poll13) — 5/5 PASS
- MAIN guests/lib (-fuzztime=5s, committed HEAD): FuzzVfioBarMapping PASS (43K/5s, 23 corpora), FuzzVfioDoorbellState PASS (42K/5s, 30), FuzzPciConfigOffset PASS (52K/5s, 4), FuzzChaosGate PASS (50K/5s, 18), FuzzParseCanonicalHeader PASS
- All targets: zero failures, no new corpus growth.

## Chaos gate
- FuzzChaosGate (re-poll13): PASS — randomized service KILL/respawn/isolation cycles exercise crash isolation (killed session's resources released, survivors keep running, respawn gets fresh SID). Practice #5 satisfied.

## Artifact freshness ledger (re-poll13) — UPDATED
- All 13 wasm artifacts now embed abi_ver=0x02 (v2.0 ABI), built from commit f6632b5.
- Hashes refreshed in verify/ARTIFACT_LEDGER.txt.
- No drift between artifacts and ratified ABI.

## Deltas reviewed this poll (re-poll13)
- MAIN@f6632b5: committed HEAD, source CLEAN. Re-reviewed core/vfio.cc (580 lines) + core/pci.cc (251 lines) for races, memory bugs, capability-check gaps, unused code, ABI deviations. Found 1 BLOCKER (F241 assignment gap), 2 MAJOR (F242 shadow buffer, F243 dead IOMMU), 5 MINOR (F244-F248).
- SVC@fb0a314: committed, stable.
- TLS@1ccd2bf: committed, stable.

## Key observations
- F241 is the highest-priority finding: the device-assignment gate that ABI §12 promises is not enforced. Any CAP_PCI session can map any device's BAR, bind its IRQ, and enable busmaster. This is both a capability-check gap (BLOCKER per severity rule) and an ABI deviation.
- F242 means the VFIO BAR mapping is currently only usable for framebuffer (where vfio_fb_present copies guest→LFB). Real device drivers (e1000.wasm, ahci.wasm) cannot reach device registers through the window.
- F243 means the IOMMU security boundary is unenforced — vfio_iommu_permits is dead code.
- All fuzz targets pass; chaos gate passes; artifact ledger is current.
- Phase 11 gate (test-p11) remains GREEN on committed HEAD.

## Process notes
- Fuzz targets run with -fuzztime=5s; 5/5 MAIN PASS.
- Never emitting ALL PHASES COMPLETE (coordinator decides).
- Branch heads: MAIN@f6632b5 (committed, clean working tree), SVC@fb0a314, TLS@1ccd2bf, lane/verify@8f1548f (re-poll13 committed).
- QA mandate satisfied: re-reviewed VFIO substrate, verified fuzz/chaos targets, updated artifact ledger.
