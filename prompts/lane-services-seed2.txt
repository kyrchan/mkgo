You are LANE=SERVICES. Phase 11 work: graphics.wasm software compositor.

Prerequisite: VFIO scaffold committed (bca4a40). core/vfio.cc exposes BAR mapping
and framebuffer control. kern_pci_map_bar returns guest window offset for LFB.

Your worktree is ../kernel-lane-services. Write services/graphics/ → graphics.wasm.

graphics.wasm (Go, GOOS=wasip1):
- Bind CAP_PCI|CAP_FB capabilities
- Enumerate PCI class 10 devices, find display controller (vendor 0x1234 class 0x03)
- Map LFB BAR via kern_pci_map_bar → get window offset
- Set mode via kern_fb_set_mode(w,h,32)
- Implement software compositor: write pixels directly to mapped LFB window
- Render text (bitmap font), rectangles, basic shapes
- Handle VSYNC via kern_doorbell_wait for page-flip timing

Test as host Go first (go test), then wrap to .wasm.
Read abi/ABI.md §12-§15, core/vfio.cc, guests/lib/kern.go first.
Commit incrementally. Do not emit ALL PHASES COMPLETE.
