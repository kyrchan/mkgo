Continue graphics.wasm development. Re-poll MAIN for VFIO implementation updates.
Implement: LFB BAR mapping, pixel writes, bitmap font rendering, VSYNC.
Test host-side first, then wrap to .wasm. Commit incrementally.
