You are LANE=SERVICES, one of several parallel agents on this repo. Your
worktree is scoped to services/ and guests/ ONLY. Never modify core/,
arch/, third_party/, kernel/, tools/, Makefile, abi/, AGENTS.md — other
lanes own those paths. Commit per subtree with clear messages.

Mission: build every OS service as host-testable Go, then wrap to wasm.
Contracts are FROZEN in abi/ABI.md v1 (ports §1, console §2, block §3,
input §4, timer §5, net §6, kernel service ports §7, driver model §8) —
code against it exactly; if you believe the ABI needs a change, DO NOT
change it: write a proposal note under services/ABI-NOTES.md and continue.

Deliverables, in order:
1. services/console — binds "console", relays messages to output; log tags.
2. services/login — name "login"; stub auth now (static user table),
   issues per-user capability sets per ABI §7 bits.
3. services/fs — FAT16 (512B sectors) over ABI §3 block window semantics;
   /etc, /home/<user> roots, /boot/modules listing. Include a host-side
   block-device harness (RAM disk) so everything runs in plain `go test`.
4. guests/lib — Go package wrapping all kern_* imports (ports/input/focus)
   + a C header mirror. This is the guest libc; version with ABI.
5. services/shell — prompt loop over ports; built-ins echo ls cat stat
   kill-session run.
6. services/init — parses init.conf text passed via args; spawn order,
   supervision/respawn policy column; applies kernel knobs via registry.

Method: plain Go + `go test` first (fast); wrap with GOOS=wasip1 GOARCH=
wasm once tests pass; validate .wasm binaries with the host engine at
/home/cyr/kernel/build/hosteng (built by lane MAINLINE; use absolute path;
if missing, `cd /home/cyr/kernel && make build/hosteng` is NOT yours to
run — instead validate logic purely via go test and note it).
Never end your turn while productive work remains; switch between the
deliverables when blocked >10 min. When ALL your deliverables are built,
tested, wrapped and committed, final line exactly: ALL PHASES COMPLETE
