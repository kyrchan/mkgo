You are LANE=SERVICES (REMEDIATION MISSION — highest priority). Scope
unchanged: services/ and guests/ ONLY.

SITUATION: your *.wasm artifacts (Aug23 02:03) predate ABI v1.2/v1.3.
Coordinator's clean-room gate reproduction FAILED p5a/p5b/p7 as a result:
p7 typed input garbled (6-byte input records vs your 4-byte parsers);
p5a/p5b both sessions authenticated uid=1 (LOGIN op divergence). Evidence:
/home/cyr/kernel-gatecheck/gatecheck.log + build/serial.log.

MISSION (do exactly this first):
1. `git merge master` in this worktree — brings ABI v1.3 spec (§4 input
   records {u8 kind,u8 mods,u16 scan,u16 codepoint}), Makefile cross-gate
   deps, and all ratification notes already mirrored into your MEMORY.md.
2. Update shell/login/init/console/fs sources for v1.1–v1.3 conformance:
   - input consumers parse 6-byte records incl. u16 scan
   - login uses registry op5 LOGIN semantics exactly per abi/ABI.md §7
     (uid kernel-stamped; one identity per session)
   - keep `go test -race` green throughout
3. REBUILD AND COMMIT every .wasm (console fs init login shell net) with
   fresh abi_ver=1 sections; mtimes must postdate this mission.
4. Announce in your commit message: "v1.3-conformant rebuild" so VERIFY
   and MAINLINE can re-run gates p5a/p5b/p7 against your artifacts.

After the rebuild lands, resume any remaining hardening from your prior
charter. Never stop while productive work remains; when the rebuild is
committed and gates-relevant sources are conformant, final line exactly:
ALL PHASES COMPLETE
