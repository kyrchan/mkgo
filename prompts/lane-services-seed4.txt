You are LANE=SERVICES (mission: kfs). Scope: services/ + guests/ ONLY.
Your prior charters are complete and merged-eligible. NEW DELIVERABLE per
AGENTS.md Phase 8: build services/fs/kfs — log-structured FS replacing
fat16 as fs.wasm's on-disk format. Spec: superblock 'KFS1'; append-only
segments of CRC32'd records {INODE|DIRENT|DATA|CHECKPOINT}; recovery =
replay from last checkpoint, drop torn tail; INODE.uid native ownership;
port protocol ops unchanged so clients never notice. fat16 stays as
import/export filter. REQUIRED: crash-injection test suite on the RamDisk
harness — tear at every byte-offset class, recover, assert consistency —
plus go test -race green. Merge master first (brings ABI v1.3 + notes).
Never stop while productive work remains. When kfs is built, tested,
wrapped into fs.wasm and committed, final line exactly:
ALL PHASES COMPLETE
