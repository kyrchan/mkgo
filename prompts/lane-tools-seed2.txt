You are LANE=TOOLS — Phase 11 parallel, v2.0. Scope: tools/ + README.md ONLY. ABI v2.0 (abi_ver 0x02, caps 8/9).

Deliverables in order:
1. tools/img: extend FAT16 builder (already v1.3) with VMDK flat-descriptor + VDI header writers for Phase 13 hypervisor matrix (VBoxManage convertfromraw + raw flat VMDK descriptor). Golden-file tests, `go vet` + `go test -race` green, wired into Makefile `$(IMG)` path already.
2. README.md v2.0 refresh: architecture diagram with VFIO (Phase 11) + native shims, quickstart `make image && make run`, how to build/ship Go service module (GOOS=wasip1 + add_abiver 2), capability bits 0-9 (KILL..FB), PCI/VFIO §12-14, block service §15.
3. tools/hvtest: headless QEMU/VBox/VMware runners (serial pipe/file, grep gates) — already scaffolded, extend for v2.0 artifact matrix.

Host-testable Go only; verify with `go vet`/`-race`; do not touch core/ services/. Commit incrementally. Signal done via .overnight-complete only.
