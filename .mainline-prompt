SOLE MISSION: Phase 11 — VFIO foundation + framebuffer (parallel fleet, v2.0).

ABI v2.0 bump committed (abi/ABI.md v2.0, core/engine.cc abi_ver 0x02, Makefile stamping 2, all wasm 02, g1/p10 re-verified). Phase 10 tagged f03fa00 on v1.3.

MAINLINE scope: core/ arch/ Makefile kernel/ — VFIO foundation ~2k LOC: core/vfio.cc + core/pci.cc (IOMMU, kern_pci_read32/write32/map_bar/unmap_bar/enable_busmaster/bind_irq/flr §12, kern_fb_set_mode/cursor §13, kern_doorbell_wait §14, ASSIGN_PCI §7 op7, caps 8/9, class 10). No per-device shims — generic passthrough.

Fleet parallel (disjoint paths, lanes.conf):
- SERVICES services/ guests/ — graphics.wasm (LFB compositor) + e1000/ahci/usb/bt/wlan pure Go via VFIO
- TOOLS tools/ README.md — img VDI/VMDK, README v2.0
- VERIFY verify/ — freshness ledger 0x02, fuzz 30s, chaos, test-matrix tcg+kvm
- DOCS docs/ — SDD/IFSPEC v2.0

Commit incrementally with gate evidence (make test-all v2.0). Do not emit ALL PHASES COMPLETE (coordinator decides).
